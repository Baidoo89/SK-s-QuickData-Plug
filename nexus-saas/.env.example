# Database
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME?schema=public"

# Auth / App base URL (required for invite links and callbacks)
NEXTAUTH_URL="https://your-domain.com"
NEXTAUTH_SECRET="replace-with-strong-random-secret"
AUTH_SECRET="replace-with-strong-random-secret"

# Email delivery (Resend)
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxx"
RESEND_FROM_EMAIL="no-reply@your-verified-domain.com"

# Optional provider connection settings
# PROVIDER_ORDER_URL="https://provider.example.com/orders"
# PROVIDER_API_KEY="replace-with-provider-secret"

# Optional fallback base URL (if NEXTAUTH_URL is not set)
# APP_URL="https://your-domain.com"
